# Тема НЕ задаётся специально — тогда Streamlit показывает в меню
# «⋮» → Settings родной переключатель Light / Dark / Use system setting,
# который корректно красит все элементы (включая таблицы).
# Графики следуют активной теме автоматически (core/ui.active_template).
[server]
headless = true
