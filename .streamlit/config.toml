# Тёмная тема — по умолчанию (нативная тема Streamlit).
# Светлую можно включить в меню «⋮» → Settings → Appearance —
# Streamlit корректно перекрасит ВСЕ элементы, включая таблицы.
[theme]
base = "dark"
primaryColor = "#4D9BE0"
font = "sans serif"

[server]
headless = true
