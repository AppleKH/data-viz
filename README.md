# 📊 Визуализация данных (аналог DataLens)

Веб-приложение на **Streamlit** для полного цикла работы с данными:
загрузка → обработка → виджеты → дашборды.

## Возможности

| Раздел | Что умеет |
|--------|-----------|
| **📥 Загрузка данных** | CSV / TSV / Excel / JSON / Parquet, подключение к SQL-базам (PostgreSQL, MySQL, SQLite) и загрузка по REST API / URL |
| **🔧 Обработка данных** | Пайплайн преобразований: фильтры, смена типов, переименование, выбор столбцов, удаление пустых/дубликатов, заполнение пропусков, сортировка, группировка с агрегацией, вычисляемые столбцы |
| **📊 Виджеты** | Линейные, столбчатые, с областями, точечные, круговые диаграммы, гистограммы, ящик с усами, KPI-показатели и таблицы |
| **📋 Дашборд** | Сборка виджетов в настраиваемую сетку, несколько дашбордов |

## Установка

```powershell
pip install -r requirements.txt
```

> Драйверы `psycopg2-binary` и `pymysql` нужны только для подключения к
> PostgreSQL / MySQL. Для SQLite ничего дополнительно не требуется.

## Запуск

```powershell
streamlit run app.py
```

Приложение откроется в браузере. Разделы — на панели слева.

## Хранение

Всё состояние лежит в каталоге `workspace/` рядом с проектом:

- `datasets/*.parquet` — сами данные;
- `datasets.json` — реестр датасетов;
- `widgets.json` — конфигурации виджетов;
- `dashboards.json` — дашборды.

Благодаря этому данные, виджеты и дашборды переживают перезапуск приложения.

## Структура

```
app.py                     главная страница
core/
  storage.py               хранение датасетов/виджетов/дашбордов
  data_sources.py          загрузка из файлов/SQL/API
  transforms.py            операции обработки данных
  charts.py                построение графиков (Plotly)
pages/
  1_Загрузка_данных.py
  2_Обработка_данных.py
  3_Виджеты.py
  4_Дашборд.py
```
